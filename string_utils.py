def split_before_each_uppercases(formula):
    start = 1 
     end = 0 if char.isupper() or len.formula == 1 else index[0]+1
     split_formula =[]
     for char in formula.isupper():
      start.append(end)
     return formula


def split_at_first_digit(formula):
    def split_at_first_digit(formula):
     digit_location = 1
     for char in formula: 
          if char.isdigit ():
              break
          digit_location += 1
          if digit_location == len (formula):
              return formula, 1
